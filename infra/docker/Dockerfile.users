FROM openjdk:17-jdk-slim

WORKDIR /app

# Copy Maven files
COPY apps/services/users-service/pom.xml .
COPY apps/services/users-service/src ./src

# Install Maven
RUN apt-get update && apt-get install -y maven && rm -rf /var/lib/apt/lists/*

# Build the application
RUN mvn clean package -DskipTests

# Run the application
EXPOSE 8083
CMD ["java", "-jar", "target/users-service-1.0.0.jar"]
