FROM openjdk:17-jdk-slim

WORKDIR /app

# Copy Maven files
COPY apps/services/orders-service/pom.xml .
COPY apps/services/orders-service/src ./src

# Install Maven
RUN apt-get update && apt-get install -y maven && rm -rf /var/lib/apt/lists/*

# Build the application
RUN mvn clean package -DskipTests

# Run the application
EXPOSE 8082
CMD ["java", "-jar", "target/orders-service-1.0.0.jar"]
